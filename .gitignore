.env.local
.env*.local
.mcp.json
node_modules/
.next/
out/
.DS_Store

# Salesforce
salesforce/.sfdx/
salesforce/.localdevserver/
salesforce/server.key

# Playwright BDD generated specs
.features-gen/

# Playwright raw test output (ephemeral — summary is in public/test-summary.json)
test-results/
playwright-report/

# Newman CI environment (generated at runtime from GitHub Secrets — never commit)
postman/ci-environment.json

# SFDX root cache
.sfdx/
salesforce/.sf/
